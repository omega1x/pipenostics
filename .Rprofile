DEFAULT_REPO <- "http://mirror.fcaglp.unlp.edu.ar/CRAN/"  ## Argentina without security check
cat(sprintf("* SGC-specific configuration: set default CRAN-repository to [%s]\n", DEFAULT_REPO))
options(repos=c(CRAN=DEFAULT_REPO))
rm(DEFAULT_REPO)
